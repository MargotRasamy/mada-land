{
  "name": "smart-contract",
  "version": "1.0.0",
  "description": "A smart-contract for a land registry system",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install:clean": "rm -rf node_modules/ && rm -rf package-lock.json && npm install"
  },
  "author": "Margot Rasamy",
  "license": "ISC",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.2",
    "hardhat": "^2.13.0",
    "dotenv": "^16.0.3"
  },
  "dependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.2",
    "hardhat": "^2.13.0",
    "dotenv": "^16.0.3"
  }
}
